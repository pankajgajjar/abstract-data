var DropDownJS = function(data){
    var data = data;
    this.createDropDown = function(dropDownObj){
        dropDownObj.innerHTML = "Current Schema: "+data;
       //Need to write code for appending the option and selecting it and making it disable
    }
}
